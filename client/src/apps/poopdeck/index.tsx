import { routes } from "./routes";

const app = {
  id: "og-poopdeck",
  label: "Poopdeck",
  description: "Internal builder application. Used to build the builder.",
  routes,
};

export default app;
